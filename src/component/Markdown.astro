---
import MarkdownIt from "markdown-it";

interface Props {
  content: string;
}
const { content } = Astro.props;

const md = new MarkdownIt();
const html = md.render(content);
---

<div class="prose md:prose-lg lg:prose-xl] mx-auto my-5" set:html={html} />
